<!DOCTYPE html>
<html>
  <head>
    <title>BMC Tracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style>
        .flash__alert   { color: red; }
        .flash__notice  { color: blue; }
        .flash__success { color: green; }
    </style>  
  </head>

  <body>
    <% if flash.any? %>
      <% flash.each do |key, value| -%>
        <section class="flash flash__<%= key %>"><%= value.html_safe %></section>
      <% end -%>
    <% end %>

    <h1>Texas A&M Badminton Club</h1>
      <h2>
        <%= link_to 'Members', members_path, class: 'button alt' %>
        <%= link_to 'Officers', officers_path, class: 'button alt' %>
        <%= link_to 'Payments', payments_path, class: 'button alt' %>
        <%= link_to 'Deposits', deposits_path, class: 'button alt' %>
        <%= link_to 'Withdrawals', withdrawals_path, class: 'button alt' %>
        <%= link_to 'Transaction_types', transaction_types_path, class: 'button alt' %>
      </h2>

    <%= yield %>
  </body>
</html>
