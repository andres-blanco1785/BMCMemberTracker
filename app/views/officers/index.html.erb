<p id="notice"><%= notice %></p>

<h1>Officers</h1>

<table>
  <thead>
    <tr> 
      <th>Officer UIN</th>
      <th>Name</th>
      <th>Email</th>
      <th>Amount Owed</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @officers.each do |officer| %>
      <tr>
        <td><%= officer.officer_uin %></td>
        <td><%= officer.name %></td>
        <td><%= officer.email %></td>
        <td><%= officer.amount_owed %></td>
        <td><%= link_to 'Show', officer %></td>
        <td><%= link_to 'Edit', edit_officer_path(officer) %></td>
        <td><%= link_to 'Destroy', officer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Officer', new_officer_path %>
